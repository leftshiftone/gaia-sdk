query {
    addPunctuationPreprocessor:boolean = false
    removeSignaturePreprocessor:boolean = false

    list(nlu(text:string, merge:boolean = false)) {
       txt:string
       raw:string
       cls(qualifier:string = "default"):list(string)
       list(lex) {
          lemma:string
          phonetic:number
          pos:string
          base:string
          flexions:list(ref(lex))
          synonyms:list(ref(lex))
          collocations:list(ref(lex))
          hyperonyms:list(ref(lex))
          meronyms:list(ref(lex))
          parts:list(ref(lex))
          abbreviations:list(ref(lex))
          causations:list(ref(lex))
          labels:list(string)
       }
       list(dep) {
          sourcePos:string
          targetPos:string
          sourceTag:string
          targetTag:string
          sourceIndex:int
          targetIndex:int
          sourceBase:string
          targetBase:string
          @Deprecated("text")
          main:boolean
          @Deprecated("text")
          modifier:boolean
          @Deprecated("text")
          parenthesized:boolean
          source:string
          target:string
          relation:string
          rawRelation:string
       }
       ner {
          list(datetime) {
             date:date
             date1:date
             date2:date
             negation:boolean
             fuzzy:boolean
             indices:list(int)
          }
          list(duration) {
             amount:int
             amountMin:int
             amountMax:int
             unit:string
             negation:boolean
             indices:list(int)
          }
          list(location(normalized:boolean = true)) {
             name:string
             type:string
             negation:boolean
             indices:list(int)
          }
          list(organization) {
             name:string
             type:string
             negation:boolean
             indices:list(int)
          }
          list(accommodation) {
             name:string
             type:string
             amount:int
             negation:boolean
             indices:list(int)
          }
          list(person) {
             name:string
             isAdult:boolean
             age:int
             negation:boolean
             indices:list(int)
          }
          list(price) {
             amount:float
             amountMin:float
             amountMax:float
             currency:string
             priceCategory:string
             sensibility:string
             scope:string
             negation:boolean
             indices:list(int)
          }
          @Deprecated("text")
          list(age) {
             age:int
             negation:boolean
             indices:list(int)
          }
          bool:list(boolean)
          email:list(string)
          url:list(string)
          list(custom(qualifier:string)) {
             data:map
             negation:boolean
             indices:list(int)
          }
       }
    }
}
